# Build artifacts
build/
cmake-build-*/
*.dSYM/

# JUCE
JuceLibraryCode/
Builds/

# macOS
.DS_Store
*.swp

# IDEs
.vscode/
.idea/
*.xcodeproj/
*.xcworkspace/

# Catch2 ignore (we vendor it as submodule)
external/Catch2/build/

# Test artifacts
tests/output/
*.junit.xml
Testing/
tests/fixtures/tts/*/audio.wav

# Ballmer chant — user-generated, not redistributed (see scripts/build_developers_clip.py)
assets/tts/01_developers/audio.wav

# Harness-managed worktrees (created by EnterWorktree)
.claude/

# Python (for tools/tts_prebake later)
__pycache__/
*.pyc
.venv/
venv/

# Piper voice files (download separately; too large to commit)
tools/tts_prebake/voices/*.onnx
tools/tts_prebake/voices/*.onnx.json
tools/tts_prebake/.venv/

# Whisper model files (download separately; too large to commit)
Resources/whisper/*.bin

# Piper binary + voices (download via scripts/fetch_piper.sh)
assets/piper/piper
assets/piper/piper_phonemize
assets/piper/espeak-ng
assets/piper/libtashkeel_model.ort
assets/piper/*.dylib
assets/piper/*.so
assets/piper/espeak-ng-data/
assets/piper/voices/*.onnx
assets/piper/voices/*.onnx.json
build-tests/
VocalSetRaw/
